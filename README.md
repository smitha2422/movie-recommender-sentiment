# Movie Recommender & Sentiment Analysis Web App

A web application built with Flask that provides movie recommendations and performs sentiment analysis on movie reviews using a machine learning model trained on the IMDB dataset.

## 🚀 Setup & Installation

1. **Clone the repository:**
   ```bash
   git clone <your-repository-url>
   cd movie-recommender-sentiment